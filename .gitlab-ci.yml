build:
  stage: build
  artifacts:
    paths:
      - src
  script:
    - cd src/client && npm install
    - cd ../server && npm install
    - cd ../client && npm run build
  
test:
  stage: test
  script:
    - echo "Nothing to test..."

deploy:
  stage: deploy
  script:
    - echo "TODO"
